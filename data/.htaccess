# Deny all access to this directory
Order deny,allow
Deny from all

# Disable PHP execution
php_flag engine off

# Prevent directory listing
Options -Indexes

# Deny access to sensitive files
<FilesMatch "\.(json|log|txt|lock)$">
    Order allow,deny
    Deny from all
</FilesMatch>
